@page
@model MatterForge.Pages.Entities.Parties.IndexModel
@{
    ViewData["Title"] = "Parties";
    ViewData["ActiveEntityTab"] = "Parties";
}

<div class="mf-page-heading">
    <div>
        <p class="mf-eyebrow">Entities</p>
        <h1>Parties</h1>
        <p class="text-muted">Searchable people, companies, aliases, and relationships used by conflict searches.</p>
    </div>
    <a class="btn btn-primary" asp-page="./Create">Create party</a>
</div>

<partial name="/Pages/Entities/_EntityTabs.cshtml" />

<div class="mf-panel">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <div>
            <strong>@(Model.Archived ? "Archived parties" : "Active parties")</strong>
            <small class="d-block text-muted">Archived parties stay in conflict searches and can be restored from their detail page.</small>
        </div>
        <div class="btn-group btn-group-sm" role="group" aria-label="Party archive filter">
            <a class="btn @(Model.Archived ? "btn-outline-secondary" : "btn-primary")" asp-page="./Index">Active</a>
            <a class="btn @(Model.Archived ? "btn-primary" : "btn-outline-secondary")" asp-page="./Index" asp-route-archived="true">Archived</a>
        </div>
    </div>

    @if (Model.Parties.Count == 0)
    {
        <p class="text-muted mb-0">No @(Model.Archived ? "archived" : "active") parties found.</p>
    }
    else
    {
        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                    <tr>
                        <th>Number</th>
                        <th>Party</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Aliases</th>
                        <th>Matter links</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var party in Model.Parties)
                    {
                        <tr>
                            <td><a asp-page="./Details" asp-route-id="@party.Id"><code>@party.PartyNumber.ToString("D8")</code></a></td>
                            <td>
                                <a class="mf-record-link" asp-page="./Details" asp-route-id="@party.Id">
                                    <strong>@party.Name</strong>
                                    <small class="d-block text-muted">@party.NormalizedName</small>
                                </a>
                            </td>
                            <td>@party.PartyType</td>
                            <td><span class="badge text-bg-secondary">@party.Status</span></td>
                            <td>@party.Aliases.Count</td>
                            <td>@party.MatterParties.Count</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
